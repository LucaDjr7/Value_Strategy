# Cœur scientifique
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7

# Accès données
wrds>=3.1                # CRSP / Compustat (nécessite un compte WRDS)
requests>=2.28          # facteurs Fama-French (Kenneth French Data Library)
pyarrow>=12.0           # cache parquet

# Optionnels — fallback automatique si absents (voir value_strategy/__init__ des modules)
hmmlearn>=0.3           # régimes HMM (sinon GaussianMixture sklearn)
xgboost>=1.7            # boosting (sinon GradientBoosting sklearn)
pandas-datareader>=0.10 # macro FRED (sinon fredapi, sinon fallback synthétique)
fredapi>=0.5

# Tests
pytest>=7.0
