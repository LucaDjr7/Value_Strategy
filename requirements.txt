# Scientific core
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7

# Data access
wrds>=3.1                # CRSP / Compustat (requires a WRDS account)
requests>=2.28          # Fama-French factors (Kenneth French Data Library)
pyarrow>=12.0           # parquet cache

# Optional — automatic fallback if absent (see the modules' value_strategy code)
hmmlearn>=0.3           # HMM regimes (otherwise GaussianMixture sklearn)
xgboost>=1.7            # boosting (otherwise GradientBoosting sklearn)
pandas-datareader>=0.10 # FRED macro (otherwise fredapi, otherwise synthetic fallback)
fredapi>=0.5

# Tests
pytest>=7.0
